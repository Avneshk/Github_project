name: first jobs

on: workflow_dispatch

jobs:
  my_first_job:
    name: my_first job
    runs-on: windows-latest
    steps:
     - name: Checkout
       uses: actions/checkout@v6.0.2
       
     - name: Creating
       run : write "Hello friends"

  my_second_job:
    name: my sec ob
    runs-on: ubuntu-latest
    needs: my_first_job
    if: always()
    steps:
     - name: creat_second
       run:  echo "Hello Avnesh"
    
       
  
