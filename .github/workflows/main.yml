name: first jobs

on: workflow_dispatch

jobs:
  my_first_job:
    name: my_first job
    runs-on: windows-latest
    steps:
     - name: Checkout
       uses: actions/checkout@v6.0.2
       
     - name: Creating
       run : write-host "hello friends"
       
  
