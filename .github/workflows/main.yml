name: first jobs

on: workflow_dispatch


jobs:
  my_first_job:
    name: my_first job
    runs-on: ubuntu-latest
    steps:
      
     - name: Checkout
       uses: actions/checkout@v6.0.2
       
     - name: Creating
       run : echo "Hello friends created by ${{ vars.NAME }}-${{RUNNER.OS}}"

  my_second_job:
    name: my sec ob
    runs-on: ubuntu-latest
    steps:
     - name: creat_second
       run:  echo "Hello my friend ${{ vars.NAME }}-${{RUNNER.OS}}"
    
       
  
